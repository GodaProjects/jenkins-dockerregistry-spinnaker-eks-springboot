apiVersion: apps/v1
kind: Deployment
metadata:
  name: godaspringboot
spec:
  selector:
    matchLabels:
      app: godaspringboot
  replicas: 1 
  template: 
    metadata:
      labels:
        app: godaspringboot
    spec:
      containers:
      - name: godaspringboot
        image: godaprojects/goda-eks-jenkins-spinnaker-poc
        ports:
        - containerPort: 8080
        imagePullPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: godaspringboot
spec:
  type: LoadBalancer
  selector:
    app: godaspringboot
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080

